(ns game.board-square-test
  (:require [clojure.test :refer :all]
            [game.board-square :refer :all]))

; (def color {:white :black})
;
; (def rank {:rook})
;
; (def dirs {:north})

; (deftest test-potential-rook-moves
;   (testing "should know the potential moves north"
;     (let [s (square 18)
;           pms (potential-moves s (:rook rank))
;           mvs (:north pms))))
